{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CodeComfy Preset",
  "description": "User-authored ComfyUI workflow preset for codecomfy-vscode.",
  "type": "object",
  "required": [
    "id",
    "name",
    "kind",
    "defaults"
  ],
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[a-zA-Z0-9_-]+$",
      "description": "Unique preset identifier. Letters, digits, underscores, and hyphens only."
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable preset name shown in the QuickPick."
    },
    "description": {
      "type": "string",
      "description": "Optional description rendered in QuickPick labels and gallery detail views."
    },
    "kind": {
      "type": "string",
      "enum": [
        "image",
        "video"
      ],
      "description": "Generation kind. Determines which command surfaces the preset."
    },
    "defaults": {
      "type": "object",
      "description": "Default input values (width, height, steps, cfg_scale, fps for video, etc).",
      "additionalProperties": true
    },
    "workflow": {
      "type": "object",
      "description": "ComfyUI workflow JSON (node-id -> node object mapping). Each node has class_type + inputs.",
      "additionalProperties": {
        "type": "object",
        "required": [
          "class_type"
        ],
        "properties": {
          "class_type": {
            "type": "string",
            "minLength": 1
          },
          "inputs": {
            "type": "object"
          }
        }
      }
    },
    "requires": {
      "type": "object",
      "description": "Model files this preset needs on the ComfyUI server. Declarative only — the workflow still carries the real filenames. Used to report a missing model by name, with a download link, before submitting.",
      "properties": {
        "note": {
          "type": "string",
          "description": "Provenance or licensing note shown in docs and QuickPick detail."
        },
        "models": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "input",
              "file",
              "folder"
            ],
            "properties": {
              "input": {
                "type": "string",
                "description": "Workflow input holding this filename, e.g. unet_name."
              },
              "file": {
                "type": "string",
                "description": "Exact filename as it appears in the loader dropdown."
              },
              "folder": {
                "type": "string",
                "description": "ComfyUI models/ subfolder, e.g. diffusion_models."
              },
              "url": {
                "type": "string",
                "description": "Direct download URL."
              }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
